---
description: 
globs: 
alwaysApply: true
---
## Always use the custom hook for tRPC client initialization
import { useTRPC } from "@/trpc/client";
const trpc = useTRPC();

const { data, isLoading } = useQuery(
  trpc.yourRouter.yourProcedure.queryOptions({
    // your parameters here
  })
);

// Use React Query's useQuery with tRPC query options
const { data, isLoading } = useQuery(
  trpc.yourRouter.yourProcedure.queryOptions({
    // your parameters here
  })
);
// For data modifications
const { mutate, isLoading } = trpc.yourRouter.yourProcedure.useMutation({
  onSuccess: () => {
    // Handle success
  },
  onError: (error) => {
    // Handle error
  }
});